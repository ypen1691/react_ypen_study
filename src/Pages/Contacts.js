import React, {Component} from 'react';

class Contacts extends Component {
    render() {
        return (
            <div>
                Hello Contacts page
            </div>
        );
    }
}

export default Contacts;